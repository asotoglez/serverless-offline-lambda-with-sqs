{
  "name": "serverless-offline-lambda-with-sqs",
  "version": "1.0.0",
  "description": "An example that shows how to run a lambda function that consumes from a local SQS queue.",
  "dependencies": {
    "serverless-domain-manager": "^5.1.0",
    "serverless-offline": "^6.8.0",
    "serverless-offline-sqs": "latest"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "CUSTOM_ENV=local serverless offline start",
    "deploy": "serverless create_domain && serverless deploy",
    "lint": "eslint ."
  }
}
